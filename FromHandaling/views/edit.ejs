<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Update User</title>
    <style>
        body {
            background-color: #121212; /* Dark background */
            color: #ffffff; /* White text */
            font-family: Arial, sans-serif;
            display: flex;
            justify-content: center;
            align-items: center;
            height: 100vh;
            margin: 0;
        }

        .form-container {
            text-align: center;
        }

        input {
            padding: 10px;
            margin: 5px;
            width: 200px;
            border: none;
            border-radius: 5px;
            outline: none;
        }

        button {
            background-color: #dbf07d;
            color: #212121;
            padding: 10px 20px;
            border: none;
            border-radius: 5px;
            cursor: pointer;
        }

        button:hover {
            background-color: #a6b564;
        }

        a {
            color: #007bff;
            text-decoration: none;
            font-size: 14px;
            margin-bottom: 10px;
            display: inline-block;
        }

        a:hover {
            text-decoration: underline;
        }
    </style>
</head>
<body>
    <div class="form-container">
        <a href="/read">Read Users</a>
        <h1>Edit yourself </h1>
        <form action="/update/<%= user._id %>" method="POST"> <!-- Update the action to POST to /update/:id -->
            <input type="text" name="name" placeholder="Name" value="<%= user.name %>" required>
            <input type="email" name="email" placeholder="Email" value="<%= user.email %>" required>
            <input type="url" name="image" placeholder="Image URL" value="<%= user.image %>">
            <br>
            <button type="submit">Update User</button>
        </form>
    </div>
</body>
</html>
