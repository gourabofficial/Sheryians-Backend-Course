<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Edit Post</title>
  <script src="https://cdn.tailwindcss.com"></script>
</head>
<body>
  <div class="flex items-center justify-center w-full min-h-screen bg-zinc-900 text-white p-3">
    <div class="w-full sm:w-1/2 lg:w-1/3 bg-zinc-800 p-6 rounded-lg shadow-md">
      <h5 class="text-xl font-semibold mb-6">Edit Your Post</h5>
      <form action="/update/<%= post._id %>" method="POST">
        <textarea
          name="content"
          placeholder="What's on your mind?"
          class="w-full p-3 rounded-md bg-zinc-700 text-white h-32 resize-none"
          required
        ><%= post.content %></textarea>
        <button
          type="submit"
          class="w-full bg-yellow-500 hover:bg-yellow-600 p-3 rounded-md text-zinc-900 font-semibold mt-4"
        >
          Update Post
        </button>
      </form>
    </div>
  </div>
</body>
</html>
